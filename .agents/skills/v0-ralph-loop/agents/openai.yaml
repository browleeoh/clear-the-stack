interface:
  display_name: "V0 Ralph Loop"
  short_description: "Run the guarded V0 issue-to-merge backlog"
  default_prompt: "Use $v0-ralph-loop to execute the authorized implementation-ready V0 ledger."
